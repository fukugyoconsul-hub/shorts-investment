# YouTubeショート動画 自走化システム仕様書(TACグループ)

最終更新: 2026-07-30

## 1. 概要

- **チャンネル**: TACグループ(YouTube、`https://www.youtube.com/@TAC_Group`、Googleアカウント: `fukugyoconsul@gmail.com`)
- **ジャンル**: FX・テクニカル分析・ファンダメンタルズ分析・CFD取引をメインに、株式/コモディティ/債券などFX以外の投資、金融、日本経済、世界情勢、時事ニュースをサブに扱う投資教育系ショート動画
- **チャンネルの位置づけ**: 相場初心者の金融・投資リテラシーを高めることが目的であり、投資の勧誘・助言を行うものではない(概要欄に毎回固定の免責文を記載)
- **目的**: 顔出し・声出しなしで、企画〜投稿〜エンゲージメント対応までを完全自動化する
- **実行環境**: GitHub Actions(クラウド)。ローカルPCの起動状態に依存せず、追加料金なしで動作する
- **姉妹システム**: エンタメ・雑学系の「ほっと一息チャンネル」(`businessfxtrader-rgb/shorts-entertainment`)と同じアーキテクチャを踏襲しているが、リポジトリ・Google Cloudプロジェクト・YouTube認証情報はすべて別物であり、一切共有しない

## 2. リポジトリ・認証情報

- **GitHubリポジトリ**: `businessfxtrader-rgb/shorts-tac-group`(非公開)
- **Google Cloudプロジェクト**: 本チャンネル専用の新規プロジェクト(「ほっと一息チャンネル」とは別。YouTube Data APIのアップロード上限が1プロジェクトあたり1日約6本のため、共有すると2チャンネル合計で上限に達する危険がある)
- **秘密情報の保管場所**: GitHub Secrets(暗号化)
  - `GOOGLE_TTS_API_KEY`: Google Cloud Text-to-Speech用(本チャンネル専用プロジェクトのキー)
  - `PEXELS_API_KEY`: Pexels動画検索用
  - `CLAUDE_CODE_OAUTH_TOKEN`: 台本・ジャンル生成用(Claude Pro、長期間有効トークン。既存チャンネルと共用可)
  - `GOOGLE_CLIENT_SECRET_JSON`: YouTube OAuthクライアント情報(本チャンネル専用プロジェクト)
  - `YOUTUBE_TOKEN_JSON`: YouTube認証トークン(TAC_Groupチャンネルで認可。スコープ: `youtube.force-ssl` + `yt-analytics.readonly`。アップロード・サムネイル・コメント・統計取得・視聴維持率取得すべてに対応。再認証は`scripts/youtube-auth.mjs`をローカルで実行しブラウザ許可)
  - `GOOGLE_SERVICE_ACCOUNT_JSON`: スプレッドシート書き込み用サービスアカウント(本チャンネル専用プロジェクトで作成し、対象スプレッドシートに編集者として共有する)

## 3. 自動実行スケジュール・バッファ方式

投稿枠は1日2回、**JST 8:00 / JST 16:00**。GitHub Actionsの`schedule`(cron)は高負荷時に遅延・スキップされることがあるため、「決まった時刻ちょうどに実行される」ことに依存せず、**予約投稿のバッファ(在庫)を切らさないこと**を目的にした設計にしている(詳細は「ほっと一息チャンネル」のSPEC.mdと同じ設計思想)。

| ワークフロー | 頻度 | 内容 |
|---|---|---|
| `daily-pipeline.yml` | 毎日 8:13・14:13・20:13・2:13(JST、6時間おき) | 「常に3日分(6本)の予約投稿」を下回っていれば、不足分だけ動画を作成してYouTubeの予約公開に登録する(バッファ方式、詳細は3-1) |
| `weekly-research.yml` | 毎週月曜 9:00(JST) | 過去動画の再生数・視聴維持率集計 + AIによる新ジャンルの自動リサーチ・追加 + トレンド調査 + インプレッション/CTR取得 + 低パフォーマンス動画の自動SEO再対策(詳細は4-1〜4-4) |
| `watchdog.yml` | 毎日 9:15・17:15(JST、投稿時刻8:00・16:00の約1時間後、1日2回。cron-job.org外部トリガー(9:03・17:03)の方が確実に発火するため、内部cronはバックアップとして後ろにずらしている) | バッファ残量チェック+予約公開の成否確認・シートのステータス確定(詳細は3-2) |

### 3-1. バッファ方式(予約投稿のストック)
- `scripts/pipeline.mjs`が実行されるたびに、`content/used-topics.json`の`publishedAt`が未来の日時になっている件数(=まだ公開されていない予約済み動画の本数)を数える
- 目標本数(3日分×1日2本=6本)に足りなければ、足りない分だけ「台本生成→TTS→背景取得→BGM選定→字幕書き出し→レンダリング→YouTubeアップロード→シート記入」を実行する。1回の実行での上限は3本(`MAX_NEW_PER_RUN`)
- YouTubeアップロード時は`privacyStatus: private` + `publishAt: <割り当てた投稿枠の日時>`で「予約公開」として登録する
- 投稿枠は「今から先の直近のJST 8:00・16:00」を古い順に割り当てる(`SLOT_HOURS_UTC = [23, 7]`。JST=UTC+9のため、08:00 JST=23:00 UTC前日、16:00 JST=07:00 UTC同日)
- ある枠の動画作成が(10回リトライしても)失敗した場合、その枠だけ管理シートに「エラー」を記録して次の枠に進む
- サムネイル設定・コメント自動返信は、予約(未公開)状態の動画にも安全に実行できるため変更なし

### 3-2. 自動実行の信頼性対策(watchdog)
`watchdog.yml`は毎日9:03・17:03 JST(投稿時刻8:00・16:00の約1時間後)に、「ほっと一息チャンネル」の設計をベースにした公開結果の判定ロジックで2つの観点をチェックする:
- **バッファ残量チェック**(`scripts/check-buffer.mjs`): 予約済み本数が警告ライン(3本)を下回っていれば、`scripts/report-status.mjs`経由で管理シートに「エラー」行を自動追加する
- **公開結果チェック**(`scripts/check-published.mjs`): 予約公開の予定時刻(30分の猶予込み)を過ぎた動画について、シート上でまだ「予約済み」のままの行だけを対象に、YouTube側の実際の`privacyStatus`を確認する
  - `public`になっていれば、管理シートのステータス列(C列)を「予約済み」→「**投稿完了**」に更新する(「ほっと一息チャンネル」側には無い、本チャンネル独自の改善点。成功時にステータスが永久に「予約済み」のまま残ってしまう不具合を修正したもの)
  - 依然として非公開のままであれば、これまで通り「エラー」に更新しK列に理由を記載する
  - シート上で既に「予約済み」以外(投稿完了・エラー等、判定済み)になっている行は再チェックしない(無駄なAPI呼び出しを避けるため)

### 3-3. 外部トリガーによる二重化(cron-job.org)
「ほっと一息チャンネル」と同様、GitHub外部の無料サービスから確実にトリガーする仕組みを併用する。

- **サービス**: [cron-job.org](https://cron-job.org/)(無料、ユーザー本人のアカウント)
- **仕組み**: GitHubのREST API(`POST /repos/{owner}/{repo}/actions/workflows/{workflow}/dispatches`)を、cron-job.org側のスケジュールから直接叩く
- **認証**: `shorts-tac-group`リポジトリのみに限定したGitHub Fine-grained Personal Access Token(権限は`Actions: Read and write`のみ)。既存チャンネル用のトークンとは別に、本チャンネル専用のトークンを新規発行する
- **設定予定ジョブ**(cron-job.orgのダッシュボードで管理):
  | ジョブ名 | 対象ワークフロー | スケジュール(UTC) | JST換算 |
  |---|---|---|---|
  | `shorts-tac-daily-pipeline-trigger` | `daily-pipeline.yml` | `20 5,11,17,23 * * *` | 14:20・20:20・2:20・8:20 |
  | `shorts-tac-watchdog-trigger` | `watchdog.yml` | `3 0,8 * * *` | 9:03・17:03(内部cronより先に発火。実測では外部トリガーの方が確実に動くため) |
- GitHub自身の`schedule`設定(3章の表)は削除せず併存させる。同時刻付近に二重発火しても、バッファが足りていれば何もせず終了するだけなので無害

## 4. コンテンツ生成の仕組み

### ジャンル(初期10種、`scripts/categories.json`で管理)
FX(kaisetsu) / テクニカル(kaisetsu) / ファンダメンタルズ(kaisetsu) / CFD(kaisetsu) / 株式(ranking) / コモディティ(ranking) / 債券(kaisetsu) / 金融(qa) / 日本経済(qa) / 世界情勢(qa)

週次リサーチ(`weekly-research.yml`)により今後も自動で増える想定(投資系の安全ルールに従った候補のみ追加)。

- ジャンル名は2〜5文字程度の短い単語1つに統一
- 詳細な生成ルールは各ジャンルの`brief`フィールドに記述(投資系特有の禁止事項も個別に明記)
- 全ジャンル`fiction: false`(投資系チャンネルでは創作要素を扱わない)
- format:rankingで客観的な記録・出来事を列挙するジャンル(株式・コモディティ・経済史)は、同じジャンルが繰り返し選ばれても矛盾しないよう、briefで「〜ベスト3」ではなく「〜3選」(数ある中から3つを選んだ、という非網羅的な言い回し)を使うよう指定している。`research-genres.mjs`にも同様の指示を組み込み、将来自動追加されるranking系ジャンルにも継承される(「ほっと一息チャンネル」で発生した「同ジャンルの複数動画が別々の『上位3つ』を主張し矛盾する」不具合の再発防止)

### フォーマット(4種類)
- **kaisetsu**(新規追加): フック(「〜とは?」の提示)→基礎→仕組み→注意点→締め、の概念解説形式。FX・テクニカル・ファンダメンタルズ・CFD・債券で使用
- **ranking**: フック→第3位→第2位→第1位→締め、のベスト3形式。株式・コモディティで使用
- **qa**: 問いかけ→結論の形式。金融・日本経済・世界情勢で使用
- **simulation**: フック→段階1→段階2→クライマックスの思考実験形式(現状の初期ジャンルでは未使用、将来のジャンル追加用に実装のみ用意)

### ナレーションの尺・密度
- 動画の尺は54〜59秒を目標(「ほっと一息チャンネル」と同じ)
- narration(読み上げ文)の合計文字数が320〜345字程度になるよう台本生成プロンプトで指定

### ジャンル選定ロジック
- 過去の平均再生数 × 視聴維持率倍率(0.5〜2.0倍)に応じた重み付き抽選(伸びている・最後まで見られているジャンルほど選ばれやすい)
- 実績の少ない/新しいジャンルにも一定の基礎重みを与え、開拓の機会を残す(コールドスタート対策)

### 4-1. SEO対策・視聴維持率トラッキング(自走PDCAループ)
「ほっと一息チャンネル」と同じ仕組みを導入。再生数だけでは「①表示されない」「②クリックされない」「③最後まで見られない」のどこがボトルネックか判別できないため、YouTube Analytics APIから**視聴維持率**を取得し、ジャンル選定と台本生成の両方に継続的にフィードバックしている。

- **データ取得**(`scripts/track-performance.mjs`、`weekly-research.yml`で週次実行):
  - `youtube.videos.list`で各動画の再生数を取得
  - `youtubeAnalytics.reports.query`で各動画の`averageViewDuration`・`averageViewPercentage`を取得(`dimensions=video`単体は"not supported"エラーになるため、必ず`filters=video==id1,id2,...`と組み合わせる)
  - 結果を`content/category-stats.json`(ジャンル別平均視聴維持率)と`content/retention-summary.json`(チャンネル全体平均)に保存
- **台本生成へのフィードバック**(`scripts/generate-script.mjs`):
  - ジャンル選定の重み = 平均再生数 × min(2.0, max(0.5, ジャンル別視聴維持率 ÷ チャンネル全体平均維持率))
  - チャンネル全体の平均視聴維持率が40%未満の場合、プロンプトに追加の強化指示(フックの具体性・情報密度・尻すぼみ防止)を自動挿入
  - 常時適用のルールとして、フック最初の1文の惹きつけ・前置き厳禁、narrationも核心を焦らしてから明かす構成を明文化
  - SEO強化: タイトル冒頭10〜15字以内にキーワード配置、タグは重要度(想定検索ボリューム)順に並べる、descriptionHookは検索結果表示を意識して具体的に、outroにコメント誘導の一言を追加(文字数上限を超える場合はチャンネル登録呼びかけを優先)
  - テロップ(caption)は音声なしでも伝わる密度にする専用ルールを追加(2行・1行10〜16字目安、核心の数字・専門用語を必ず含める)
  - 感嘆符・疑問符はプロンプト指示だけでなく、生成結果に対してコード側(`normalizePunctuation`)で確実に全角へ変換する(プロンプト指示のみでは徹底されないことがあったため)

### 4-2. トレンド調査(週次・自動)
`scripts/research-trends.mjs`(`weekly-research.yml`で週次実行)が、Web検索を使ってFX・投資教育系Shortsで今伸びている「構造パターン」(タイトルの型・フックの作り方等)と「テーマの傾向」を調査し、`content/trend-insights.json`に保存する。特定動画の台詞・構成の丸写しは禁止し、抽象化した一般的な例のみを扱う(著作権配慮)。投資系の安全ルール(売買助言につながる切り口の禁止)も明記済み。`generate-script.mjs`が毎回ランダムに1パターン・1テーマだけを取り込み、不自然にならない場合のみ採用する。

### 4-3. 低パフォーマンス動画の自動SEO再対策(自走PDCAループ)
`scripts/auto-retry-seo.mjs`(`weekly-research.yml`で週次実行)が、伸び悩んでいる動画のタイトル・タグ・概要欄冒頭を自動で改善する。

- 対象: 公開から2日以上経過した動画のうち、「1日あたり再生数」がチャンネル中央値(2日以上経過した動画のみで算出)の50%未満のもの
- 再試行後は、前回リトライ「後」の増分ペースで再評価する(公開時からの累計平均のままだと、改善前の低調な実績でいつまでも薄まってしまうため)
- 同じ動画への再試行は7日間のクールダウンを空ける
- 1回の実行での上限は3本(API消費・コストの安全策)
- 内容(トピック)は変更せず、投資系の安全ルールを踏まえてタイトル・タグ・descriptionHookのみをClaudeで再生成し、YouTube・管理シート両方に反映する
- 手動実行用の`scripts/retry-seo.mjs`(対象動画IDを指定して個別に実行)も別途用意している

### 4-4. インプレッション・クリック率トラッキング
`scripts/track-reach.mjs`(`weekly-research.yml`で週次実行)が、YouTube Reporting API(`youtubereporting.googleapis.com`、通常のAnalytics APIとは別のAPI)経由で、一般クリエイター向けにも公開されているインプレッション数・クリック率(CTR)のバルクレポート(1〜2日遅れ、CSV形式)を取得し、`content/reach-stats.json`に保存する。初回セットアップとして、本チャンネル専用のGoogle Cloudプロジェクトで同APIを有効化し、`reporting.jobs.create({reportTypeId: "channel_reach_basic_a1"})`でジョブを1回だけ作成済み(JOB_IDはスクリプト内にハードコード)。

### ナレーター(ランダム、速度統一)
- 「キャラクター設定:ランダム」の要望に対応するため、Google Cloud TTSの`ja-JP-Chirp3-HD-*`ボイスから動画ごとにランダムに1つを選択する(`scripts/generate-tts.mjs`)
- ボイスプール(8種、性別・トーンが異なるものを選定): Aoede・Kore・Leda・Autonoe(女性)/ Charon・Fenrir・Puck・Algenib(男性)
- 読み上げ速度(`speakingRate`)は聞きやすさのため、ボイスに関わらず`1.15`で統一

### 投資系チャンネル特有の安全策(最重要)
台本生成(`generate-script.mjs`)・週次リサーチ(`research-genres.mjs`)の両方のプロンプトで、以下を絶対禁止事項として明記している:
- 「今買うべき」「今が売り時」等、具体的な売買タイミングの示唆・個別銘柄/通貨ペアの売買推奨
- 特定の金融商品の将来価格・値動きの断定的な予測
- 「必ず儲かる」「絶対に稼げる」等、利益・成功を保証する表現
- 個別の資産配分・具体的投資判断そのものへのアドバイス(一般的な仕組み・過去の事実の解説に留める)
- 実在のアナリスト・トレーダー・インフルエンサー等の実名・個人の投資成績
- 特定の金融機関・証券会社・取引所の推奨または批判
- 著作権のあるキャラクター・作品への言及
- 全ジャンル`fiction: false`とし、創作要素は扱わない
- 誤読防止辞書(`content/misreading-dict.json`)に金融用語(為替・利回り・含み損・建玉・追証など)を追加済み

これらは概要欄に毎回固定で記載される免責文(6章参照)と合わせて、二重の安全策になっている。

## 5. 素材生成

| 要素 | 使用サービス | 詳細 |
|---|---|---|
| ナレーション | Google Cloud TTS(Chirp3-HD、8種のボイスからランダム選択) | 無料枠(月100万文字)の範囲内 |
| 背景動画 | Pexels API | 各シーンの内容に応じた英語キーワードで検索・縦向き動画を自動取得 |
| BGM | 自前ライブラリ6曲(`scripts/bgm-library.json`) | すべてKevin MacLeod氏の楽曲、CC BY 3.0。曲ごとに動的にクレジット表記を概要欄へ挿入(「ほっと一息チャンネル」と共通のライブラリ) |

## 6. 書き出し

- Remotion(1080×1920、30fps)。レンダリング部分(`src/`)は「ほっと一息チャンネル」と共通の汎用実装(チャンネル固有のブランディングなし)

## 7. 公開時の処理(YouTube Data API)

- **公開設定**: 予約公開(`privacyStatus: private` + `publishAt`)
- **カテゴリ**: エンターテインメント(categoryId: 24)
- **子ども向け設定**: 否(`selfDeclaredMadeForKids: false`)
- **概要欄**: 内容要約 + 固定紹介文(投資リテラシー向上目的の説明・メインチャンネル/LINE/ニコニコ/X/Instagram/お問い合わせのリンク)+ アフィリエイトリンク(FX会社・バイナリーオプション会社・過去検証ソフト)+ 免責事項 + BGMクレジット + ハッシュタグ(`description-template.txt`)
- **ハッシュタグ**: 固定4つ(#FX #テクニカル分析 #ファンダメンタルズ分析 #shorts)+ 動画内容に応じた動的5つ
- **タグ**: 動画ごとに具体的なキーワードを8個程度
- **SEO対策メモ**: 今回何をSEO対策したかを1〜2文で生成し、管理シートに記録
- **サムネイル**: 動画の一部フレームを静止画として書き出し、自動設定

## 8. 記録・分析

- **管理シート**: ユーザー提供の共有スプレッドシート内、**「TACグループ」タブ**(サービスアカウント経由で書き込み。スプレッドシートIDは「ほっと一息チャンネル」と同一ドキュメントだが、タブが異なるため、`report-status.mjs`・`append-sheet.mjs`・`check-published.mjs`・`retry-seo.mjs`では`SHEET_NAME`を`"TACグループ"`に固定し、タブの並び順(`sheets[0]`等)には一切依存しない設計にしている。「ほっと一息チャンネル」側で実際にタブ追加によりこの問題が発生した経緯があるため、本チャンネルでは構築当初からこの対策を組み込み済み)
  - 列構成は「ほっと一息チャンネル」と同じ: A=No. / B=動画形式 / C=ステータス / D=ジャンル / E=投稿日時 / F=タイトル / G=概要欄全文 / H=ハッシュタグ / I=動画URL / J=SEO対策メモ / K=エラー内容・備考
  - **ステータス(C列)**: 動画作成・予約登録が正常完了した時点で自動で「予約済み」。その後、watchdogの公開結果チェック(`check-published.mjs`)が予約公開の予定時刻を過ぎた時点で実際の公開状況を確認し、正常に公開されていれば「**投稿完了**」に、公開されていなければ「エラー」に自動更新する。想定外の事象の場合は「保留」も使用する
  - 書き込み処理はA列に一切触れない
- **再生数トラッキング**(`content/category-stats.json`): 週次でジャンル別の平均再生数を集計し、次回以降のジャンル選定に反映

### 8-1. 失敗時の自動リトライ
「ほっと一息チャンネル」と同じ仕組み: 各工程は失敗しても1〜3分(ランダム)待って最大10回まで自動的に再試行し、10回すべて失敗した場合のみ管理シートに「エラー」として記録する(`scripts/pipeline.mjs`の`runWithRetry`)。サムネイル設定・コメント自動返信は失敗しても後続処理をブロックしない「ソフト」実行。

## 9. エンゲージメント対応

- **コメント自動返信**(`scripts/reply-comments.mjs`)
  - 1回の実行で最大3件まで返信
  - 返信文言は投資系チャンネル向けに書き直し、X誘導文言には`https://x.com/TAC_FXtrade`を記載
  - X誘導あり10パターン/なし4パターンからランダム選択、確率はX誘導あり70%・なし30%
  - 返信済みコメントは`content/replied-comments.json`で管理し、二重返信を防止

## 10. 既知の制限・保留事項

- **YouTube Data APIの無料枠上限**: 1日あたり約6本のアップロードが上限(1プロジェクトあたり)。本チャンネルは専用のGoogle Cloudプロジェクトを使うため、「ほっと一息チャンネル」の投稿量とは影響し合わない
- **Instagram同時投稿**: 未対応(対象外として保留中)
- **サムネイル**: YouTube Shorts本編のフィード上には表示されないが、検索結果・チャンネルページ・関連動画には反映される。A/Bテストの仕組みは未実装
- **競合・トレンド動画の個別分析**: `research-trends.mjs`による一般的な傾向調査は自動化済みだが、特定の競合動画URLを個別に分析する仕組みはなく、必要な場合は都度手動で行う
